---

- hosts: all
  become: true
  tasks: 
  
  - name: install apache on all host
    yum:
      name: httpd
      state: latest

  - name: Start service httpd, if not started
    ansible.builtin.service:
      name: httpd
      state: started
      enabled: yes
